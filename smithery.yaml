runtime: container
name: crew-optimizer
displayName: Crew Optimizer MCP
description: CrewAI-based LP simplex and MILP branch-and-bound tools exposed via MCP.
startCommand:
  type: "http"
  commandFunction: |
    (config) => ({
      command: "python",
      args: ["-m", "crew_optimizer.server"],
      env: {}
    })
port: 3333
configSchema:
  type: object
  properties: {}
  additionalProperties: false
