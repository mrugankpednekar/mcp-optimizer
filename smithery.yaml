runtime: "container"
build:
  dockerfile: "Dockerfile"
  dockerBuildPath: "."
startCommand:
  type: "http"
  command:
    - "python"
    - "-m"
    - "crew_optimizer.server"
configSchema:
  type: object
  properties: {}
  additionalProperties: false
