version: 1
type: remote
runtime: python
pythonVersion: "3.11"
installCommands:
  - pip install --upgrade pip
  - pip install .
startCommand:
  command: ["/app/.venv/bin/python", "-m", "mcp_optimizer.server"]
