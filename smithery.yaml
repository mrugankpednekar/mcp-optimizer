version: 1
type: remote
runtime: python
pythonVersion: "3.12"
installCommands:
  - pip install --upgrade pip
  - pip install .
  - cp .smithery/startup.sh /usr/local/bin/startup.sh
  - chmod +x /usr/local/bin/startup.sh
startCommand:
  command: ["/usr/local/bin/startup.sh"]
